import {notFound} from "next/navigation"
import {money,products} from "@/lib/products"
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();return <main className="section shell"><div className="network"><div className="thumb" style={{minHeight:420}}>공식 상품 이미지 연결 예정</div><div><div className="eyebrow">{p.brand} · {p.model}</div><h1>{p.name}</h1><h2>{money(p.price)}</h2><p className="sub">실제 모델/가격이 검증된 시작 상품입니다. 현재 재고와 수령 가능 시각은 운영 DB 연결 전에는 확정 표시하지 않습니다.</p><hr/><h3>권함</h3><p>정확한 규격을 알고 빠르게 다시 필요한 경우.</p><h3>STATION</h3><p>경기도 · 포천 운영 기반 / 경기도 · 구리 서비스 예정 권역</p><button style={{height:48,padding:"0 24px",background:"#1a1a1a",color:"white",border:0}}>장바구니 기능 · Medusa Cart 연결 단계</button></div></div></main>}
